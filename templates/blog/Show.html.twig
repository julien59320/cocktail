{% extends 'base.html.twig' %}

{% block body %}

{% for post in post %}
	<article class="post-page">
		<div class="content">
			<img src="{{ post.image }}" alt="">
			<h2 class="title">{{ post.title }}</h2>
		</div>
		#}
		{{ post.content| raw }}

	</div>
</article>{% endfor %}</article>{% endblock %}
